# Python
__pycache__/
*.pyc
backend/venv/
backend/temp/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backend/temp_uploads/*
docker-compose.local.yml

# ── Secrets & environment ─────────────────────────────────────────────
# Asla commit etme — .env.example şablonu kullanılır.
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# ── Database files, dumps, backups ────────────────────────────────────
# pg_dump çıktısı, SQLite dosyaları, vb. — içlerinde API key olabilir.
*.db
*.db-journal
*.sqlite
*.sqlite3
*.dump
db_yedek*.sql
backups/

# ── Local secret keys (Fernet master key vs.) ─────────────────────────
*.key
secret.txt
secrets/
